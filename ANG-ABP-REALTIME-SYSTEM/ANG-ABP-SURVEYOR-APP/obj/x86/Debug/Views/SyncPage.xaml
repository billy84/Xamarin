<Page x:ConnectionId='1'
    x:Name="pageRoot"
    x:Class="ANG_ABP_SURVEYOR_APP.Views.SyncPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ANG_ABP_SURVEYOR_APP.Views"
    xmlns:common="using:ANG_ABP_SURVEYOR_APP.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"                     >

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">Syncing</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <Grid Margin="10" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="8*"/>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="8*"/>
                <ColumnDefinition Width="8*"/>

            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30*"/>
                <RowDefinition Height="100*"/>
                <RowDefinition Height="100*"/>
                <RowDefinition Height="100*"/>
                <RowDefinition Height="70*"/>

            </Grid.RowDefinitions>
            <ListView x:Name="lvProjects" Margin="10" Grid.Row="1" Grid.ColumnSpan="4" Grid.RowSpan="3" BorderBrush="#FF999595" BorderThickness="2" SelectionMode="Multiple">

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="40" Width="{Binding ListViewWidth}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  Background="#FF3A3838" >

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="8*"/>
                                <ColumnDefinition Width="20*"/>
                                <ColumnDefinition Width="8*"/>
                                <ColumnDefinition Width="8*"/>

                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" HorizontalAlignment="Stretch" Text="{Binding ProjectNo}" FontSize="24" FontFamily="Global User Interface" />
                            <TextBlock Grid.Column="1"  HorizontalAlignment="Stretch" Text="{Binding ProjectName}" FontSize="24" />
                            <TextBlock Grid.Column="2" HorizontalAlignment="Stretch" TextAlignment="Center" Text="{Binding SubProjectQtyDisplay}" FontSize="24" FontFamily="Global User Interface"  />
                            <TextBlock Grid.Column="3" HorizontalAlignment="Stretch"  Text="{Binding Status}" TextAlignment="Center" FontSize="24" FontFamily="Global User Interface" />

                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
            <TextBlock Margin="25,0,10,10" TextWrapping="Wrap" Text="Project No" FontSize="28"/>
            <TextBlock Margin="9,0,10,10" TextWrapping="Wrap" Text="Project Name" FontSize="28" Grid.Column="1"/>
            <TextBlock Margin="10,0,10,10" TextWrapping="Wrap" Text="Sub-Project Qty" FontSize="28" Grid.Column="2"/>
            <TextBlock Margin="9,0,10,10" TextWrapping="Wrap" Text="Sync Status"  FontSize="28" Grid.Column="3"/>
            <Grid Margin="10,0" Grid.Row="4" Grid.ColumnSpan="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="8*"/>

                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="100*"/>
                    <RowDefinition Height="60*"/>

                </Grid.RowDefinitions>
                <Button x:ConnectionId='2' x:Name="btnSelectAll" Content="Select All" HorizontalAlignment="Stretch" Margin="7" VerticalAlignment="Stretch" FontSize="20"                           />
                <Button x:ConnectionId='3' x:Name="btnDeSelectAll" Content="De-Select All" HorizontalAlignment="Stretch" Margin="7,7,6,7" VerticalAlignment="Stretch" FontSize="20" Grid.Column="1"                             />
                <Button x:ConnectionId='4' x:Name="btnStartSyncing" Content="Start Syncing" HorizontalAlignment="Stretch" Margin="7" VerticalAlignment="Stretch" FontSize="20" Grid.Column="2"                              />
                <TextBlock x:Name="tbSyncStatus" Margin="10,0" Grid.Row="1" TextWrapping="Wrap" Text="Ready..." Grid.ColumnSpan="4" TextAlignment="Center" FontSize="24"/>
                <Button x:ConnectionId='5' x:Name="btnDeleteProject" Content="Delete Project" HorizontalAlignment="Stretch" Margin="6,7,7,7" VerticalAlignment="Stretch" FontSize="20" Grid.Column="3"                               />

            </Grid>

        </Grid>
    </Grid>
</Page>

