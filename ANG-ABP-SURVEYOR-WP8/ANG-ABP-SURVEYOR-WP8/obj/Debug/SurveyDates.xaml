<Page
    x:Class="ANG_ABP_SURVEYOR_WP8.SurveyDates"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ANG_ABP_SURVEYOR_WP8"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.BottomAppBar>
        <CommandBar>

            <AppBarButton x:ConnectionId='1' Label="Select All" Icon="SelectAll"                         />
            <AppBarButton x:ConnectionId='2' Label="DeSelect All" Icon="ClearSelection"                     />
            <AppBarButton x:ConnectionId='3' Label="Set Date" Icon="Calendar"                              />
            <AppBarButton x:ConnectionId='4' Label="UnConfirm" Icon="Undo"                         />

        </CommandBar>
    </Page.BottomAppBar>

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock Text="Survey Dates Search Results" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>            
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->
        <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0">

            <ListView Background="DarkGray" x:Name="lvToday" Margin="10,0,10,10">

                <ListView.ItemTemplate>
                    <DataTemplate>

                        <Grid>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="14*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="20*"/>
                                <RowDefinition Height="10*"/>
                                <RowDefinition Height="10*"/>
                                <RowDefinition Height="10*"/>  
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="1" >
                                <TextBlock  Text="{Binding Address}" TextWrapping="WrapWholeWords" FontSize="26" />
                                <TextBlock  Text="{Binding PostCodeAppointment}" FontSize="20" />
                                <TextBlock  Text="{Binding ProgressStatus}" FontSize="20" />
                                <TextBlock  Text="{Binding Confirmed}" FontSize="20" />
                                
                                <TextBlock Text="" FontSize="20" />

                            </StackPanel>

                        </Grid>

                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>


        </Grid>
    </Grid>
</Page>

