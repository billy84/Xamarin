<Page
    x:Class="ANG_ABP_SURVEYOR_WP8.LoginFirstOption"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ANG_ABP_SURVEYOR_WP8"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.BottomAppBar>
        <CommandBar>

            <AppBarButton x:ConnectionId='1' Label="Download" Icon="Download"                              />
            <AppBarButton x:ConnectionId='2' Label="Sync" Icon="Sync"                          />            
            <AppBarButton x:ConnectionId='3' Label="Settings" Icon="Setting"                       />

        </CommandBar>
    </Page.BottomAppBar>
    
    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock Text="Main Menu Options" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->
        <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0">

            <Grid.RowDefinitions>
                <RowDefinition Height="30*"/>
                <RowDefinition Height="30*"/>
                <RowDefinition Height="30*"/>
                <RowDefinition Height="30*"/>
                <RowDefinition Height="30*"/>
            </Grid.RowDefinitions>
            
            <Button x:Name="btnSetSurveyDates" Content="Set Survey Dates" HorizontalAlignment="Stretch" Margin="10,-0.1,10,1.2" Grid.Row="1" VerticalAlignment="Stretch"/>
            <Button x:Name="btnProcessSurveys" Content="Process Surveys" HorizontalAlignment="Stretch" Margin="10,0.8,10,0.3" Grid.Row="2" VerticalAlignment="Stretch"/>

        </Grid>
    </Grid>
</Page>

